# Private Outreach Recipients

`private_outreach_recipients.example.json` is a shape example only. Copy it to
`private_outreach_recipients.json` only when the user has manually verified real
public team emails.

Rules:

- Do not commit `private_outreach_recipients.json`.
- Do not add personal maintainer emails.
- Do not scrape GitHub user emails.
- Do not use security emails for generic protocol pitch.
- Do not use `security-reports@cursor.com` unless reporting a real
  vulnerability.
- Use public channels first for Cursor Origin, Graphite, and MCP.
