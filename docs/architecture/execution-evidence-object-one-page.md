# Execution Evidence Object One-Page Overview

Historical note: this one-page overview belongs to the earlier
`Execution Evidence Object` surface. Treat it as lineage material; use Agent
Evidence / AEP v0.1 as the current primary entry.

**English**  Execution Evidence Object One-Page Overview
**中文**  执行证据对象一页总览。

## Key Message / 核心说明

**English**  Execution Evidence Object turns runtime activity into a portable and verifiable object.
**中文**  执行证据对象把运行时活动变成一个可移植、可验证的对象。

## TODO Asset / 待放入图片
- **EN**: Insert `docs/assets/execution-evidence-object-flow.png` here later.
- **中文**：之后在这里插入 `docs/assets/execution-evidence-object-flow.png`。

## Six-step Flow / 六步流程
- **EN**: Agent run → runtime events → execution evidence object → verification → FDO-style mapping → portable audit artifact.
- **中文**：Agent run → runtime events → execution evidence object → verification → FDO-style mapping → portable audit artifact。

## Mapping Table / 映射表
- **EN**: Agent run = runtime source; object = bounded payload; verification = integrity check; mapping = object shell; audit artifact = reusable review result.
- **中文**：Agent run 是运行来源；object 是有边界载荷；verification 是完整性检查；mapping 是对象外壳；audit artifact 是可复用审阅结果。

## Closing / 结尾
- **EN**: This page explains why the repository is a standards specimen rather than only a toolkit.
- **中文**：这页说明了为什么这个仓库是一个标准样品，而不只是工具库。
