# Development Ledger

This ledger records meaningful project changes that affect humans or agents
trying to understand, cite, validate, or call `agent-evidence`.

It is not a typo log. Record changes when they alter behavior, callable
surfaces, metadata, policies, artifact semantics, validation expectations, or
agent-facing discovery.

## Current Focus

The current wave adds agent-native discovery, citation, attribution,
recommendation policy, development-ledger discipline, metadata validation,
structured CLI capabilities metadata, integration terminology alignment, and
generated agent metadata surfaces.

## What Changes Should Be Recorded

- CLI, API, MCP, or other callable-surface changes
- schema, profile, validator, or evidence-integrity changes
- citation, attribution, DOI, license, version, or repository metadata changes
- recommendation, promotion, or claim-boundary policy changes
- agent-facing discovery changes in `AGENTS.md`, `llms.txt`, or docs
- meaningful integration maturity changes
- release, archival, or reproducibility evidence changes

## Ledger

| Date | Change ID | Area | Summary | Agent-Facing Impact | Verification | Evidence Integrity Impact | Next Action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-05-03 | agent-native-p0-project-facts | discovery | Added `docs/project-facts.md` as the canonical source for project facts. | Agents have one source for name, version, DOI, license, repository, CLI, integrations, and boundaries. | Metadata and tests checked in this PR wave. | Reduces drift between docs and callable metadata. | Keep this file updated when project facts change. |
| 2026-05-03 | agent-native-p1-discovery | discovery | Added agent-facing discovery guidance in `AGENTS.md`, `llms.txt`, and `docs/for-agents.md`. | External agents can decide when to use or avoid the project. | `pytest -q` and metadata validation in this PR wave. | Improves correct use of validation and export boundaries. | Keep discovery docs aligned with `docs/project-facts.md`. |
| 2026-05-03 | agent-native-p2-citation-attribution | metadata | Added citation, CodeMeta, attribution, and recommendation policy files. | Agents can cite and attribute the project without inventing metadata or promotion mechanics. | CFF/YAML and JSON parsing checks. | Makes derived work attribution auditable and explicit. | Update citation metadata when DOI, version, or author metadata changes. |
| 2026-05-03 | agent-native-p3-ledger-ci | governance | Added development ledger, PR template, and metadata validation CI. | Agents and reviewers get machine-checkable discovery and policy invariants. | Local metadata script and GitHub Actions workflow. | Preserves metadata integrity and claim boundaries. | Extend checks when callable surfaces change. |
| 2026-05-03 | agent-native-p4-cli-capabilities | callable-surface | Add structured `agent-evidence capabilities --json` output and callable-surface docs. | Agents can inspect current capabilities without scraping README prose. | `agent-evidence capabilities --json \| python -m json.tool` and `pytest -q`. | Keeps wrappers and external agents aligned with implemented surfaces. | Keep OpenAPI and MCP marked unavailable until real wrappers exist. |
| 2026-05-03 | p5-integration-terminology-normalization | integrations | Normalized LangChain, OpenAI Agents, and CrewAI integration terminology around the agent-native discovery layer. | Agents reading integration docs see the same runtime evidence exporter boundary as `AGENTS.md` and `llms.txt`. | `pytest -q`, targeted integration terminology checks, `agent-evidence capabilities --json \| python -m json.tool`, and ruff. | Reduces drift between example integrations and canonical evidence/export boundaries. | Keep integration docs aligned when maturity or callable surfaces change. |
| 2026-05-03 | p6-generated-agent-metadata | metadata | Added generated `agent-index.json`, `llms-full.txt`, schema, generation scripts, and drift tests. | Agents get deterministic machine-readable and expanded LLM-readable metadata derived from existing project facts and capabilities. | `pytest -q`, generation `--check` commands, schema validation, metadata validation, capabilities JSON parsing, and ruff. | Reduces metadata drift across project facts, citation metadata, policy files, and callable-surface metadata. | Keep generated metadata checked whenever facts, policy, citation, or callable surfaces change. |
| 2026-05-03 | p7-lite-local-openapi-wrapper | callable-surface | Added a stdlib local OpenAPI wrapper with `agent-evidence serve`, health, capabilities, profile validation, and bundle verification endpoints. | Agents can call a local HTTP wrapper while CLI/core behavior remains canonical. | `pytest -q`, `agent-evidence capabilities --json \| python -m json.tool`, generation `--check` commands, OpenAPI contract tests, local API tests, and ruff. | Keeps callable HTTP access aligned with existing validation and verification logic without changing evidence semantics. | MCP remains unavailable; no telemetry, auth system, hosted mode, or Review Pack endpoint was added. |
| 2026-05-03 | p8-local-mcp-readonly-tools | callable-surface | Added a local stdio MCP wrapper with read-only / verify-first tools and fixed read-only resources. | Agents can inspect capabilities, schemas, docs, citation policy, and run validation or verification through MCP without new evidence semantics. | `pytest -q`, MCP boundary tests, `agent-evidence capabilities --json \| python -m json.tool`, generation `--check` commands, and ruff. | Keeps MCP aligned with existing CLI/core validation and verification paths. | Keep MCP local stdio only; do not add telemetry, registry publication, prompts, write tools, or Review Pack tools without a separate plan. |
| 2026-05-03 | p9-langchain-five-minute-runnable-path | integrations | Clarified the LangChain 5-minute runnable path around the existing offline/mock example, signed export bundle, verification command, and summary artifact. | Agents can direct users to one copy/paste LangChain path that requires no external API key and produces reviewable evidence artifacts. | `pytest -q`, LangChain subprocess smoke test, `verify-export` CLI test, metadata generation `--check`, and ruff. | Improves reproducibility of the first integration entrypoint without changing schema, core validation, OpenAPI, or MCP behavior. | Review whether this path is sufficient before planning OpenAI-compatible adapter work. |
| 2026-05-03 | p10-openai-compatible-minimal-evidence-path | integrations | Added a provider-agnostic OpenAI-compatible minimal evidence path with mock/offline default behavior, signed export bundle, verification command, and summary artifact. | Agents can direct users to a second copy/paste runtime evidence path that uses only `api_key`, `base_url`, and `model` for live provider configuration while requiring no external API key by default. | `pytest -q`, OpenAI-compatible subprocess smoke test, `verify-export` CLI test, metadata generation `--check`, and ruff. | Improves reproducibility of the second integration entrypoint without changing schema, core validation, OpenAPI, or MCP behavior. | Review whether this path is sufficient before planning any live-provider or OpenAI-compatible adapter expansion. |
| 2026-05-03 | p11-openai-compatible-path-hardening | integrations | Hardened the OpenAI-compatible mock/live boundary with no-network, no-secret-leak, and live configuration error tests plus docs-only provider configuration examples. | Agents can recommend the OpenAI-compatible path with clearer safety boundaries: mock/offline by default, explicit live opt-in, and no provider secrets in generated artifacts. | `pytest -q`, OpenAI-compatible hardening tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, and ruff. | Reduces accidental network use and provider secret leakage risk without changing schema, core validation, OpenAPI, or MCP behavior. | Keep provider examples docs-only unless a separate live-provider plan is approved. |
| 2026-05-03 | p12-release-prep-v0.3.0 | release | Prepared v0.3.0 release metadata, release notes, readiness status, release checklist, and surface classification after the P0-P11 work. | Agents can distinguish supported, beta, experimental, and planned surfaces without overclaiming OpenAPI, MCP, Review Pack, AI Act Pack, or registry status. | `pytest -q`, release metadata consistency tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, smoke checks, and ruff. | Aligns version, citation, generated metadata, and release-facing docs without changing schema, core validation, OpenAPI behavior, or MCP behavior. | Confirm DOI handling, GitHub release metadata, and PyPI publication during release execution. |
| 2026-05-03 | p13-1-doi-metadata-patch-v0.3.1 | release | Prepared a metadata-only v0.3.1 patch to correct DOI strategy after Zenodo archived GitHub Release v0.3.0. | Agents should use the Zenodo concept DOI as the primary project DOI and use the v0.3.0 version DOI only for exact release citation. | `pytest -q`, release metadata consistency tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, DOI metadata checks, and ruff. | Removes a stale v0.2.0 version DOI from active citation metadata without changing schema, core validation, OpenAPI behavior, or MCP behavior. | Publish v0.3.1 only after release authorization and PyPI/TestPyPI credentials are ready. |
| 2026-05-03 | p16-review-pack-v0.1 | review-pack | Added local Review Pack V0.1 creation for verified signed export bundles. | Agents can produce reviewer-facing local packs after signed export verification without new evidence semantics. | `pytest -q`, Review Pack tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, smoke checks, and ruff. | Verifies before packaging, copies only public artifacts, avoids private keys, network access, legal/compliance claims, OpenAPI/MCP behavior changes, and schema/core changes. | Run post-merge Review Pack audit before release prep. |
| 2026-05-03 | p18-review-pack-release-prep-v0.4.0 | release | Prepared v0.4.0 release metadata, release notes, readiness status, checklist, and surface classification for Review Pack V0.1. | Agents can distinguish Review Pack V0.1 as local/offline reviewer packaging while keeping legal, compliance, AI Act, hosted-service, OpenAPI, and MCP boundaries explicit. | `pytest -q`, release metadata consistency tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, Review Pack smoke checks, and ruff. | Aligns version, citation, generated metadata, and release-facing docs without changing schema, core validation, OpenAPI behavior, MCP behavior, or Review Pack behavior. | Enter v0.4.0 release authorization gate after post-merge release-prep review. |
| 2026-05-03 | p22-review-pack-v0.2 | review-pack | Implemented Review Pack V0.2 as a narrow stabilization of local reviewer packaging. | Agents can produce clearer reviewer-facing packs with checklist, artifact inventory, findings tables, and explicit limitations while keeping Review Pack local and offline. | `pytest -q`, Review Pack V0.2 tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, smoke checks, and ruff. | Enhances reviewer summaries, small findings taxonomy, and manifest/receipt clarity without OpenAPI/MCP behavior changes, schema/core changes, legal/compliance claims, or release/version bump. | Run post-merge audit before any release-prep decision. |
| 2026-05-03 | p25-review-pack-v0.2-release-prep-v0.5.0 | release | Prepared v0.5.0 release metadata, release notes, readiness status, checklist, and generated metadata for Review Pack V0.2. | Agents can identify Review Pack V0.2 as beta local/offline reviewer packaging while seeing the package, citation, and generated metadata aligned on v0.5.0. | `pytest -q`, release metadata consistency tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, Review Pack V0.2 smoke checks, build, twine check, and ruff. | Aligns release metadata without changing Review Pack behavior, schema, core validation, OpenAPI behavior, or MCP behavior. | Enter v0.5.0 release-prep post-merge smoke before release authorization. |
| 2026-05-04 | p28-1-docs-only-post-release-correction-v0.5.0 | release | Corrected docs-only post-release drift after v0.5.0 publication. | Agents see v0.5.0 as the current released version, cite the concept DOI as primary project DOI, and use version DOIs only for exact release citation. | `pytest -q`, release metadata consistency tests, capabilities JSON parsing, metadata generation `--check`, stale wording checks, DOI docs checks, and ruff. | Fixes stale v0.4.0 and release-prep wording without code changes, version bump, OpenAPI/MCP changes, schema/core changes, or publishing. | Decide separately whether PyPI long-description drift warrants a v0.5.1 docs-only release. |
| 2026-05-04 | p31-review-pack-v0.3 | review-pack | Implemented Review Pack V0.3 as a narrow stabilization of local reviewer packaging. | Agents get stable reviewer checklist IDs, local_offline pack metadata, conservative secret scan status, and optional structured Review Pack failure output. | `pytest -q`, Review Pack V0.3 tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, smoke checks, and ruff. | Improves reviewer-facing package clarity without package version bump, release notes, OpenAPI/MCP behavior changes, schema/core changes, or legal/compliance claims. | Run post-merge audit before any release-prep decision. |
| 2026-05-04 | p34-review-pack-v0.3-release-prep-v0.6.0 | release | Prepared v0.6.0 release metadata, release notes, readiness status, checklist, and generated metadata for Review Pack V0.3. | Agents can identify Review Pack V0.3 as beta local/offline reviewer packaging while seeing the package, citation, and generated metadata aligned on v0.6.0. | `pytest -q`, release metadata consistency tests, `agent-evidence capabilities --json \| python -m json.tool`, metadata generation `--check`, Review Pack V0.3 smoke checks, build, twine check, and ruff. | Aligns release metadata without changing Review Pack behavior, schema, core validation, OpenAPI behavior, MCP behavior, or publishing. | Enter v0.6.0 release-prep post-merge smoke before release authorization. |
| 2026-05-04 | p37-1-docs-only-post-release-correction-v0.6.0 | release | Corrected docs-only post-release drift after v0.6.0 publication. | Agents see v0.6.0 as the current released version, cite the concept DOI as primary project DOI, and use the v0.6.0 version DOI only for exact release citation. | `pytest -q`, release metadata consistency tests, capabilities JSON parsing, metadata generation `--check`, stale wording checks, DOI docs checks, ledger JSONL parse, and ruff. | Fixes stale v0.6.0 release-prep wording without code changes, version bump, OpenAPI/MCP changes, schema/core changes, DOI strategy changes, or publishing. | Decide separately whether PyPI long-description drift warrants a docs-only release. |
| 2026-05-04 | p40-strategic-positioning-docs | docs | Added docs-only strategic positioning material after the v0.6.0 release. | Agents can distinguish current capabilities, Review Pack V0.3 positioning, Operation Accountability Profile framing, FDO/data-space mapping boundaries, and paper-outline direction without inventing new claims. | `pytest -q`, capabilities JSON parsing, metadata generation `--check`, ledger JSONL parse, `git diff --check`, and ruff. | Documents existing evidence and review boundaries without code changes, release changes, OpenAPI/MCP changes, schema/core changes, or AI Act Pack implementation. | Use these positioning docs before planning paper drafting or any later AI Act Pack work. |
| 2026-05-04 | p42-review-pack-v0.3-technical-note-draft | docs | Added a docs-only technical note draft for Review Pack V0.3. | Agents and reviewers get a citable draft explaining the Review Pack artifact model, fail-closed verification flow, checklist IDs, findings/status model, agent-native use, and boundaries. | `pytest -q`, capabilities JSON parsing, metadata generation `--check`, ledger JSONL parse, boundary grep, `git diff --check`, and ruff. | Summarizes existing validation evidence and research framing without code changes, release changes, OpenAPI/MCP changes, schema/core changes, or AI Act Pack implementation. | Review and refine the technical note before any paper-format or submission planning. |
