# LangChain Integration

This prototype integration captures a minimal LangChain runtime trace,
converts it into an Execution Evidence Object, and exports a JSON evidence
bundle.

Run:

```bash
python integrations/langchain/export_evidence.py
```
