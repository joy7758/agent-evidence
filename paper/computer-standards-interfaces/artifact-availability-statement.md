# Artifact Availability Statement

Status: draft wording for author review. Use only the version that matches the
actual artifact state at submission time.

## A. Conservative Local-only Version

The paper artifact is currently available as a local repository-contained
artifact for author review. A local sealed tag, `eeoap-v0.1-paper`, exists as
an artifact anchor, with sealed artifact commit
`96f444b7ed39b39fe9f47e428af835952e843cb0`. This statement does not claim that
the tag has been pushed publicly.

The artifact remains scoped to the `examples/paper_case/` files, the local
validator path, the `make paper-demo` reproduction command, the valid evidence
PASS result, the tampered-output FAIL result, the
`references_digest_mismatch` failure code, and targeted EEOAP tests previously
reported as `19 passed, 1 warning`.

No public GitHub Release is claimed. No Zenodo DOI (Digital Object Identifier,
数字对象标识符) is claimed. The artifact is not claimed to be production-ready,
an official FDO (FAIR Digital Object, 公平数字对象) standard implementation or
adoption, a legal compliance mechanism, or a ZKP (Zero-Knowledge Proof,
零知识证明) implementation.

## B. Future Public-release Version

Use this version only after a public GitHub Release, archive, or DOI actually
exists. Replace bracketed placeholders only with verified public metadata.

The paper artifact is publicly available at `[verified release or archive URL]`.
The release/archive corresponds to tag `[verified public tag]` and commit
`[verified public commit]`. If a DOI has actually been issued, cite it here:
`[verified DOI]`. If no DOI has been issued, state that no DOI is available.

The public artifact remains scoped to the EEOAP paper case, local validator
path, valid PASS result, tampered FAIL result, `references_digest_mismatch`,
and targeted EEOAP tests. Publication of the artifact does not claim
production readiness, official FDO (FAIR Digital Object, 公平数字对象) standard
adoption, legal compliance, ZKP (Zero-Knowledge Proof, 零知识证明)
implementation, semantic correctness of AI (Artificial Intelligence,
人工智能) output, or full repository pytest success unless those facts are
separately verified and explicitly supported.
