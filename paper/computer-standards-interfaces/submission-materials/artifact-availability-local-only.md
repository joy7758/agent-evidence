# Artifact Availability - Local-only

The local sealed tag `eeoap-v0.1-paper` exists as a local artifact anchor for
the EEOAP paper artifact. The sealed artifact commit is
`96f444b7ed39b39fe9f47e428af835952e843cb0`.

This statement does not claim that the tag has been pushed publicly. No public
GitHub Release is claimed. No Zenodo DOI is claimed.

The artifact is scoped to:

- `paper_case` artifacts.
- the local validator path.
- the `make paper-demo` reproduction command.
- valid evidence PASS.
- tampered output FAIL.
- the expected failure code `references_digest_mismatch`.
- targeted EEOAP tests previously reported as `19 passed, 1 warning`.

If required by editors or reviewers, a private review package may be supplied
through the journal workflow.

The artifact is not claimed to provide production readiness, legal
compliance, official FDO standard adoption, certification, conformance,
endorsement, ZKP implementation, semantic correctness of AI output, or full
repository pytest success.
