# Minimal runtime-module sample properties for the agent-evidence EDC spike.
# exporter.type is intentionally omitted here so the extension keeps its default filesystem handoff.

web.http.port=19191
web.http.path=/api

edc.agent-evidence.output-dir=build/runtime-module-sample-output
